// Keep this in sync with pubspec.yaml version.
// The app compares this against the Supabase app_versions table
// to detect when a new update is available.
const int currentBuildNumber = 2;
const String currentVersion = '1.0.0';
